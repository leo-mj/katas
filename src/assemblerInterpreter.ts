export function assemblerInterpreter(program: string): string|-1 {
    const programLines: string[] = program.split("\n");
    if (!programLines.includes("end")) {
        return -1;
    }
    
    return -1
}

